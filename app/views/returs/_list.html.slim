tr
  td #{retur.invoice}
  td #{retur.store.name}
  td #{retur.supplier.name}
  td #{retur.total_items}
  td #{retur.date_created.to_date}
  td
    - if retur.date_approve.nil?
      | - 
    - else
      | #{order.approved_by.name} (#{retur.date_approve.to_date})
  td
    - if retur.date_picked.nil?
      | - 
    - else
      | #{order.picked_by.name} (#{retur.date_picked.to_date})
  td
    - if retur.status.nil?
      | -
    - else
      | #{retur.status.to_date}

  - if retur.date_picked.nil?
    td
      = form_for :retur, url: retur_path(id: retur.id), method: "DELETE" do |form|
        = form.submit "Hapus", class: "btn btn-rounded btn-danger"
  td
    a href='#{retur_items_path(id: retur.id)}'
      button class="btn btn-rounded btn-primary"
        | Item

